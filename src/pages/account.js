import React from "react"

// NB: gatsby turns all files in the pages folder into a route that exports a react component.
//
const Account = () => (
  <div>
    <p>This is going to be a protected route.</p>
  </div>
)

export default Account
